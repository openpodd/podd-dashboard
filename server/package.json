{
  "name": "mock-server",
  "version": "0.0.1",
  "description": "just a mock-server (temporary)",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache 2.0",
  "dependencies": {
    "fibers": "^1.0.2",
    "socket.io": "^1.2.0"
  }
}
